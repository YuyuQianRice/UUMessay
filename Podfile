# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UUMessay' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UUMessay

  target 'UUMessayTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UUMessayUITests' do
    # Pods for testing
  end
  # firebase
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  # facebook sign in
  pod 'FBSDKLoginKit'
  # google sign in
  pod 'GoogleSignIn'
  
end
